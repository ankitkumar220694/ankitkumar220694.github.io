# Résumé

Drop your résumé PDF here as **`Ankit-Kumar-Resume.pdf`** (exact filename).

The site already links to `/assets/resume/Ankit-Kumar-Resume.pdf` from:
- the **Download Résumé** button on the About page
- the résumé icon in the sidebar contact rail

Once the PDF is committed at that path, both links work with no further changes.
To use a different filename, update `resume_url` in `_config.yml`.
